# 小向超市客户服务端 PRD

| 项目 | 内容 |
|---|---|
| 产品名称 | 小向超市客户服务端 |
| 当前版本 | V1.x（线上运行版） |
| 运行形态 | 纯前端单文件 `index.html` + Node `server.js`，部署于云服务器 |
| 对外地址 | https://xxhrcs.com（手机/电脑浏览器均可访问，双端数据互通） |
| 目标用户 | 到店顾客（自助）+ 店主/店员（管理模式维护数据） |
| 管理口令 | 默认 123456，可在系统设置修改 |

---

## 1. 产品目标和使用场景
- 面向到店顾客的轻量服务端，顾客无需登录注册即可自助完成：逛商品、查快递、看二手寄售。
- 店长/店员在同一站点进入管理后台维护商品、快递、寄售与店铺信息。
- 数据集中保存在云服务器，电脑关机也能访问，手机/电脑实时互通、不回档。
- 页面自适应手机与电脑。

## 2. 整体页面框架和导航结构
- 顶部店铺信息条：📍 F7-1 37街3号；电话 📞03175333055、📞03119990792（居中、tel: 可点击）。
- 底部 Tab 导航：首页 / 商品 / 快递 / 寄售。
- 管理后台通过"管理模式"入口进入，含四个 Tab：商品管理 / 快递管理 / 寄售管理 / 系统设置。
- 主题切换：夜晚星空（流星）/ 白天柔和风景，选择被记忆。

## 3. 首页内容结构
- 三张透明背景卡片：商品展示、快递查询、二手寄售，点击进入对应模块。
- 每张卡片显示动态数量统计。
- 顶部店铺信息条。

## 4. 每个模块的用途
- 商品展示：按分类浏览店内商品，查看图片、介绍、库存；价格为 0 不显示价格。
- 快递查询：顾客查询店内代收快递状态，可发起/撤回取件申请。
- 二手寄售（闲鱼风双列卡片）：浏览在售寄售物品，圆形 + 号发布寄售申请。
- 管理后台：上架商品、登记/编辑快递、管理寄售、店铺设置。

## 5. 第一版最必要的数据和操作
### 商品
- 字段：名称、分类、图片、介绍、价格（<=0 隐藏）、单位、是否有货。
- 分类预设：蔬菜水果 / 零食饮料 / 日用百货 / 烟 / 酒槟榔。
- 顾客：分类切换、名称实时搜索、全部/有货/缺货筛选。
- 管理员：新增/编辑/删除商品。

### 快递
- 字段：运单号、收件人昵称、备注、取件编号、到店时间、状态。
- 状态流：待取 → 取件中（顾客点击发起，记录申请时间）→ 已取件（管理员审核）；另有"国内"状态。
- 顾客操作：实时搜索（昵称/单号/后四位）、编号单独搜索、按状态筛选、按到店日期检索、按编号/到店时间排序（可切升降序，默认编号升序）、点击发起/撤回取件申请、一页 10 条翻页。
- 管理员操作：单个登记、批量快速添加（每行一个单号）、📷 照片 OCR 识别单号、📊 导入 Excel；列表内编辑（单号/状态/备注/编号/昵称/到店时间）、标记已取走、驳回申请、删除；实时搜索单号/编号/昵称；取件中记录自动置顶；一页 10 条翻页。

### 二手寄售
- 顾客：双列卡片浏览，点卡片看详情；圆形 + 号发布寄售（名称/描述/价格/电话/称呼/图片）；"我要购买"后留买家手机号，卖家凭自设密码查看买家联系。
- 管理员：审核/管理寄售物品。

## 6. 首页各模块之间的关系
- 三张卡片是三个独立模块的入口，数据各自独立，仅在首页聚合展示数量。
- 全部写入经由管理后台，顾客侧浏览/发起申请。

## 7. 必须完成的功能
- 商品浏览/实时搜索/有货缺货筛选，管理员商品增删改。
- 快递登记/批量/OCR/Excel 导入、状态流转、顾客取件申请、搜索排序翻页、取件中置顶。
- 寄售浏览/发布/购买留电/卖家密码查看。
- 双端实时数据互通（服务器按 id 合并 + 显式 `_del` 墓碑删除；本地改动 5 秒内不被覆盖）。
- 昼夜主题切换。
- 所有搜索框实时搜索、无需回车、点击不丢焦点。

## 8. 暂时不做的功能
- 在线支付、购物车、订单结算。
- 用户账号体系（注册/登录）。
- 真实物流轨迹查询。
- 多店铺/多门店。

## 9. 本地数据文件、备份和恢复要求
- 线上数据集中存于云服务器 `data.json`，Node + pm2 托管。
- 代码在 GitHub 仓库 main 分支；部署：`git pull && pm2 restart supermarket`。
- 前端 localStorage 仅作离线缓存，不作为权威数据源。
- 管理员可在系统设置导出/导入数据做备份。

## 10. 可以实际检查的验收标准
| # | 验收项 | 检查方法 |
|---|---|---|
| 1 | 站点可访问 | https://xxhrcs.com 正常打开，手机/电脑样式自适应 |
| 2 | 商品 | 分类切换、实时搜索、有货/缺货筛选均生效；价格为 0 不显示 |
| 3 | 快递状态流 | 顾客点"待取"变"取件中"并记录时间，可撤回；管理员标记后变"已取件" |
| 4 | 快递后台 | 登记/批量/OCR/Excel 导入可用；编辑各字段生效；取件中在后台置顶 |
| 5 | 快递搜索排序 | 顾客端与后台搜索实时过滤、不丢焦点、无需回车；编号/到店时间排序可切换 |
| 6 | 寄售 | 发布后顾客端可见；详情不显示卖家电话；购买留电后卖家凭密码可看 |
| 7 | 双端互通 | A 端新增/删除，B 端 1 秒内同步，不回档、不乱删 |
| 8 | 主题 | 夜晚星空/白天风景可切换，刷新后保持 |
| 9 | 输入安全 | 特殊字符原样显示，不执行 HTML |

---

## 附录：需求变更记录
- 最初为本地单文件应用（商品/快递/汇率三模块），后汇率模块整体替换为"二手寄售"。
- 从本机 localStorage 演进为云服务器集中数据，解决多端不同步与回档问题。
- 部署从 Render/Koyeb（无国际信用卡）改为腾讯云香港轻量服务器 + 自有域名 xxhrcs.com + HTTPS。
