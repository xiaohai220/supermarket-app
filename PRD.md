# 超市客户服务终端 · 产品需求文档（PRD）

| 项目 | 内容 |
|---|---|
| 产品名称 | 超市客户服务终端（Supermarket Customer Service Terminal） |
| 版本 | V1.0（第一版） |
| 运行形态 | 本机浏览器运行的本地网页应用（单 HTML 文件） |
| 目标用户 | 超市客户（自助使用）+ 店主/店员（管理模式维护数据） |
| 本文档用途 | 后续开发的唯一需求依据；已确认内容以此为准 |

---

## 1. 产品目标和使用场景

### 1.1 产品定位

在超市店内电脑上运行、供**客户自助使用**的服务终端。客户无需登录、无需店员协助，即可完成三件事：**逛商品、查快递、看汇率/换汇**。店主/店员通过「管理模式」维护商品、快递与汇率设置。

### 1.2 核心目标

1. 客户在店内能自助、直观地完成三类高频服务，减少服务台排队；
2. 全程免登录、免注册，界面**中英双语可切换**；
3. 数据**只保存在本机**，不依赖云服务；除汇率获取外**完全离线可用**；
4. 刷新、关闭、重启电脑后数据**不丢失**。

### 1.3 使用场景

| 场景 | 用户 | 行为 |
|---|---|---|
| 逛商品 | 客户 | 进店按分类浏览商品、搜索名称、看价格/单位/库存 |
| 查快递 | 客户 | 到快递代收点，输入**运单号**查询自己的件是否已被取走 |
| 看汇率 | 客户 | 查看国际货币兑本地货币的最新汇率 |
| 换汇 | 客户 | 输入金额、选择原币种与目标币种进行换算，并可保存换算历史 |
| 维护数据 | 店主/店员 | 管理模式中增删改商品、登记快递、更新取件状态、设置汇率币种、备份数据 |

### 1.4 硬性约束（已与用户确认）

- **仅本地运行**：双击 HTML 文件在浏览器（推荐 Chrome/Edge）打开，不需要服务器、部署、云数据库；
- **数据存本机**：使用浏览器本地存储（localStorage），刷新/关闭/重启均不丢失；
- **免登录**：无账号体系；管理模式通过「口令」进入（默认 123456，可修改）；
- **联网策略**：唯一联网点是汇率自动获取；断网时汇率模块**仍可查看缓存、仍可换算**，但必须**红色醒目标注「未联网 · 非在线汇率」**（已确认的最终策略，覆盖此前"必须联网"的旧要求）；
- **不做手机端**（但页面需在窄屏下不破版）。

---

## 2. 整体页面框架和导航结构

单页应用（单文件 `index.html`），采用 **hash 路由**切换视图。

### 2.1 页面清单

| 路由 | 页面 | 入口 | 访问条件 |
|---|---|---|---|
| `#/home` | 首页 | 默认页 / 各子页「返回」 | 所有人 |
| `#/products` | 商品展示 | 首页卡片 | 所有人 |
| `#/parcels` | 快递查询 | 首页卡片 | 所有人 |
| `#/rates` | 货币汇率（含换汇+历史） | 首页卡片 | 所有人 |
| `#/admin?tab=products` | 管理模式 · 商品管理 | 首页底部「管理模式」 | 需口令 |
| `#/admin?tab=parcels` | 管理模式 · 快递管理 | 管理模式 Tab | 需口令 |
| `#/admin?tab=rates` | 管理模式 · 汇率设置 | 管理模式 Tab | 需口令 |
| `#/admin?tab=settings` | 管理模式 · 系统设置 | 管理模式 Tab | 需口令 |

### 2.2 全局导航元素

- **顶栏**（所有子页）：左侧「返回」按钮 + 页面标题；右侧「语言切换」按钮（中文 ⇄ English），语言选择持久化；
- **管理模式入口**：仅首页底部显示「管理模式」，点击后弹出口令输入框；口令正确进入管理模式，未登录直接访问 `#/admin` 会被拦截回首页；
- 管理模式内顶部为 4 个 Tab：商品管理 / 快递管理 / 汇率设置 / 系统设置。

### 2.3 导航规则

- 客户从首页三张卡片进入对应模块，模块内「返回」回到首页；
- 管理模式进入后 Tab 之间可自由切换，需要返回客户侧时点「返回」；
- 语言切换在任何页面即时生效并持久化。

---

## 3. 首页内容结构

自上而下：

1. **顶栏**
   - 超市名称（取自系统设置，中/英各一条，默认「社区超市 / Community Supermarket」）；
   - 副标题「客户服务终端」+ 当天日期（如 2026年9月21日 星期一）；
   - 右上角语言切换按钮。
2. **装饰条**（条形码风格的细条，品牌化元素）。
3. **三个模块卡片**（横排，窄屏自动换行），每张卡片包含：图标、模块名称、一句话用途说明、**动态统计**、进入按钮：

   | 卡片 | 用途说明 | 动态统计 |
   |---|---|---|
   | 商品展示 🛒 | 浏览店内商品，按分类查找、搜索名称、查看价格与库存 | 当前商品总数「N 件商品」 |
   | 快递查询 📦 | 输入快递运单号，查询快递是否已被取走 | 当前待取件数「N 件待取」 |
   | 货币汇率 💱 | 查看国际货币最新汇率，支持自定义换汇 | 最近一次汇率更新时间 |

   > 注：界面不使用 emoji 图标，实际以统一风格的内联 SVG 图标呈现。

4. **底部**
   - 「管理模式」入口（锁形图标 + 文字，点击弹口令框）；
   - 提示文案「数据仅保存在本机浏览器，不会上传」。

---

## 4. 每个模块的用途

### 4.1 商品展示（Products）

客户进店浏览商品的窗口：按分类筛选、按名称搜索、按价格排序，查看商品名称、价格、单位、库存状态，帮助客户快速定位目标商品。

### 4.2 快递查询（Parcels）

超市代收快递的自助查询台：客户输入**运单号**，查询该快递是否到店、是否已被取走，以及货架位置。**客户只能查，不能改**。

### 4.3 货币汇率（Rates）

店内汇率看板 + 换汇工具：展示主要国际货币兑本地货币（基准货币，默认 PKR）的最新汇率，支持**自定义换汇**（金额 + 原币种 + 目标币种实时换算）与**换算历史**（可删除/清空）。

---

## 5. 第一版最必要的数据和操作

### 5.1 商品展示

| 数据字段 | 说明 |
|---|---|
| 名称（中文） | 必填 |
| 名称（英文） | 选填 |
| 分类 | 必选（分类在系统设置中维护） |
| 价格 | 数字，单位 = 基准货币（默认 PKR） |
| 单位 | 如 500g / 个 / 瓶（选填） |
| 库存状态 | 有货 / 缺货 |
| 商品图片 | 选填；上传后压缩（≤420px 宽，JPEG）存入本机 |

客户操作：分类筛选、名称搜索（中英文）、价格排序（默认/升/降）、查看。
管理操作：新增、编辑、删除。

### 5.2 快递查询

| 数据字段 | 说明 |
|---|---|
| 运单号 | 必填，同一店唯一（不区分大小写，保存为统一大写） |
| 货架号 | 选填（如 A-3） |
| 到店时间 | 默认当前时间，可改 |
| 状态 | 待取 / 已取走 |
| 取走时间 | 标记已取走时自动记录 |

客户操作：输入运单号查询（精确匹配），显示状态、货架号、到店时间、取走时间；未查到给出提示。
管理操作：登记（支持连续登记）、标记已取走、撤销标记、按状态筛选、按单号搜索、删除。

### 5.3 货币汇率

| 数据字段 | 说明 |
|---|---|
| 基准货币 | 默认 PKR，可在汇率设置中修改 |
| 显示货币列表 | 管理端勾选要展示的币种 |
| 汇率缓存 | { base, rates, updatedAt }，联网获取后缓存 |
| 换汇历史 | { 金额, 原币种, 目标币种, 汇率, 结果, 时间 }，最多保留 50 条 |

客户操作：查看汇率列表、手动刷新、自定义换汇（金额/原币种/目标币种/一键互换）、记录本次换算、删除单条历史、清空全部历史。
管理操作：设置基准货币、勾选显示币种、手动刷新汇率。

### 5.4 汇率数据口径（重要实现要点，后续开发不可改错）

汇率接口（open.er-api.com）返回的 `rates[T]` 含义是 **1 单位基准货币 = rates[T] 单位 T**（例如 PKR 基准下 `rates.USD = 0.003598`，即 1 PKR = 0.0036 USD）。因此：

- 列表展示「1 T = X 基准」时：`X = 1 / rates[T]`；
- 自定义换汇 X→Y 的汇率：`rate = rates[Y] / rates[X]`，结果 = 金额 × rate。

> 历史上曾因公式方向写反产生错误结果（显示"1 USD = 0.36 PKR"），已用真实接口数据修正并测试通过，后续开发必须保持此口径。

---

## 6. 首页各模块之间的关系

1. **并列独立**：三个模块服务三种独立客户场景，互不依赖、互不跳转；
2. **共享配置**：语言设置（全局）、基准货币（商品价格与汇率模块共用同一货币代码）、本地存储共用同一数据区；
3. **统一维护入口**：三块数据的写入全部经由「管理模式」，客户侧只读；
4. **信息联动**：首页三张卡片展示的动态统计（商品数 / 待取件数 / 汇率更新时间）分别来自三个模块的数据，让客户进店一眼看到关键信息。

---

## 7. 第一版必须完成的功能

1. 三个客户模块完整可用：
   - 商品展示：分类筛选 + 搜索 + 价格排序 + 价格/单位/库存展示 + 商品图片；
   - 快递查询：运单号查询 + 状态/货架/时间展示 + 未找到提示；
   - 货币汇率：自动联网获取 + 手动刷新 + 缓存兜底 + 自定义换汇 + 换算历史（记录/删除/清空，上限 50 条）；
2. **汇率离线标注（最终策略）**：断网时汇率列表与换汇**仍可用缓存数据**，但必须**红色标注「未联网 · 非在线汇率」**（红色边框 + 红色状态文字 + 红色提示条）；联网成功后显示绿色「已使用最新在线汇率」并去掉红色标注；完全无缓存时才禁用换汇并提示；
3. **防反复报错**：联网失败后自动重试需有退避（≥1 分钟），不产生无限重试循环；手动刷新随时可用；
4. 管理模式（口令进入，默认 123456，可修改）：
   - 商品管理：增删改（含图片上传压缩、库存切换）；
   - 快递管理：登记（含连续登记）、标记已取走/撤销、筛选、搜索、删除；
   - 汇率设置：基准货币、显示币种勾选、刷新；
   - 系统设置：超市名称（中/英）、修改口令、分类管理（增/改名/删除，被商品占用时禁止删除）、数据备份；
5. 本地持久化：全部数据存 localStorage，刷新/关闭/重启不丢失；
6. 备份与恢复：一键导出 JSON 备份（含设置/商品/快递/汇率/换汇历史），一键导入恢复（覆盖当前数据）；
7. 中英双语：全界面文案双语，一键切换并持久化；
8. 输入安全：所有用户输入经 HTML 转义后渲染（防 XSS）；运单号去空格大写归一。

---

## 8. 第一版暂时不做的功能

| 类别 | 说明 |
|---|---|
| 账号体系 | 登录、注册、会员、积分、优惠券、支付 |
| 汇率扩展 | 汇率历史走势图、K 线、更多图表 |
| 多端/多店 | 手机原生版、多店数据同步、云端备份 |
| 硬件对接 | 扫码枪、小票打印、快递柜对接 |
| 营销功能 | 促销活动、秒杀、推荐算法 |

---

## 9. 本地数据文件、备份和恢复要求

### 9.1 本地存储设计

数据全部存放在浏览器 **localStorage**（单文件应用，无独立数据库文件），键名与内容：

| 键名 | 内容 |
|---|---|
| `sm_settings` | 语言、管理口令、基准货币、显示币种、超市名称（中/英）、商品分类 |
| `sm_products` | 商品数组（含压缩后的图片 dataURL） |
| `sm_parcels` | 快递数组 |
| `sm_rates` | 汇率缓存 { base, rates, updatedAt } |
| `sm_conv_history` | 换汇历史数组（上限 50） |

### 9.2 存储注意事项（需在界面提示用户）

- 浏览器本地存储容量有限（约 5MB），商品图片已压缩以降低占用；写入失败（空间不足）时给出明确提示，并提示删除部分商品图片；
- **清除浏览器数据、更换浏览器或电脑都会导致数据丢失**，因此必须提供备份导出功能并提示用户定期备份。

### 9.3 备份与恢复要求

- **导出备份**：将全部数据（设置/商品/快递/汇率/换汇历史）打包为 JSON 文件下载，文件名形如 `supermarket-backup-YYYYMMDD.json`；
- **导入恢复**：选择备份文件，校验格式后覆盖当前全部数据，并即时生效；
- 备份与恢复入口位于「管理模式 → 系统设置 → 数据备份」；
- 备份文件格式异常时给出明确错误提示，不覆盖现有数据。

---

## 10. 可以实际检查的验收标准

以下每条均为可实际执行的检查项，全部通过视为第一版验收通过：

| # | 验收项 | 检查方法 |
|---|---|---|
| 1 | 本地运行 | 双击 `index.html` 能在 Chrome/Edge 中正常打开，无报错 |
| 2 | 三个模块入口 | 首页三张卡片可分别进入商品展示、快递查询、货币汇率，并可返回首页 |
| 3 | 商品展示可用 | 添加 3 个商品后：分类筛选、搜索、价格排序均生效；价格显示含基准货币代码；有货/缺货徽标正确 |
| 4 | 快递查询可用 | 登记一个运单号后，客户输入该单号显示「待取」及到店时间；标记已取走后显示「已取走」及取走时间；输入不存在单号显示「未找到」提示 |
| 5 | 汇率联网获取 | 联网状态下进入汇率页自动获取，显示绿色「已使用最新在线汇率 · 更新于…」，1 USD 显示值 ≈ 实际行情（1 USD ≈ 278 PKR 量级） |
| 6 | 断网红色标注 | 断开网络（或关闭联网）后刷新：汇率列表显示缓存数据且为**红色**「未联网 · 非在线汇率」；换汇仍可计算，换算卡为红色边框并有红色提示条 |
| 7 | 换算正确性 | 用真实汇率验证：100 USD→PKR ≈ 27,7XX PKR；PKR→USD 正确；两种外币交叉换算（如 USD→EUR）正确 |
| 8 | 换算历史 | 「记录本次换算」后历史新增一条（含金额/结果/汇率/时间）；单条删除、清空（带确认）均生效；上限 50 条 |
| 9 | 管理模式门禁 | 未输口令访问 `#/admin` 被拦截；输入默认口令 123456 可进入；修改口令后旧口令失效 |
| 10 | 商品管理 | 可新增/编辑/删除商品（含图片上传）；被删除商品不再出现在客户商品页 |
| 11 | 快递管理 | 可登记、标记已取走、撤销、按状态筛选、搜索、删除 |
| 12 | 汇率设置 | 修改基准货币后，汇率列表与换汇均按新基准展示；显示币种勾选生效 |
| 13 | 系统设置 | 超市名称（中/英）修改后首页即时生效；分类可增/改名/删除，被占用分类删除被阻止 |
| 14 | 数据持久化 | 添加商品/快递/历史后：刷新页面、关闭并重新打开页面、重启浏览器，数据仍在 |
| 15 | 备份/恢复 | 导出 JSON 文件；清空数据后导入该文件，全部数据恢复；导入非法文件有错误提示且不破坏现有数据 |
| 16 | 双语切换 | 任意页面切换中文/English，界面文案全部切换，语言选择在刷新后保持 |
| 17 | 自动重试退避 | 断网状态下不产生高频无限重试（1 分钟内自动重试不超过 1 次），手动刷新按钮始终可用 |
| 18 | 输入安全 | 商品名、运单号等含 `<`、`>`、引号等字符时，页面正常显示原文而不执行 HTML |

---

## 附录 A：需求变更记录

| 时间 | 变更 | 结果 |
|---|---|---|
| 需求确认阶段 | 汇率联网策略确认 | 最初确认「允许联网自动获取」 |
| 迭代中 | 汇率换算结果错误 | 修正数据口径与换算公式（见 5.4），用真实接口数据验证 |
| 迭代中 | 换算必须联网 | 曾改为「在线才可换算」 |
| 迭代中（最终） | 断网可换算，但红色标注 | **当前生效策略**：断网用缓存可换算，红色标注「未联网 · 非在线汇率」；联网成功显示绿色 |

## 附录 B：当前实现状态

第一版已完成开发并通过自动化验证（JS 语法检查 + 逻辑冒烟测试），产物为单文件应用：

- 文件路径：`C:\Users\Administrator\Desktop\APP\supermarket-app\index.html`
- 本 PRD 存放：`C:\Users\Administrator\Desktop\APP\supermarket-app\PRD.md`
